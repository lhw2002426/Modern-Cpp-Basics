为什么以值类型传递`std::string_view`，而不是以`const std::string_view&`？多考虑一些可能的原因。

//string_view指向的内容可以改变x

按值往往比按 const& 更快（尤其在 ABI 调用约定下）

很多主流 ABI（SysV x86-64、AArch64 等）对小的 POD/Trivial 类型：

按值：可能直接通过寄存器传参（或高效的栈传递）

按 const&：需要传一个指针（引用本质上是指针），然后函数体内再多一次间接访存解引用

对“两个机器字”的小对象而言，按值往往更容易被编译器优化，减少一次内存间接访问，甚至能更好地内联与常量传播。